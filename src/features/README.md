# Feature Folder Layout

- `home/`: production-ready homepage sections and components.
- `enterprise/`: reserved for Enterprise route implementation.
- `news/`: reserved for News route implementation.
- `careers/`: reserved for Careers route implementation.

Each feature should keep its own UI components, local styles, and data/config files in the same directory to reduce cross-team merge conflicts.
